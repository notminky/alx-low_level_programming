#include <stdio.h>
/**
	*main- prints the alphater in lower case.
*
*
*Return: awlays0.
*/
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
		putchar(letter);

	putchar('\n');

	return (0);
}
